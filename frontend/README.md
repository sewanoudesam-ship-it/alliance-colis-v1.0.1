# Alliance Colis — Frontend

Voir le README à la racine du projet (`../README.md`) pour l'installation complète
(base de données Supabase, Edge Functions de paiement, variables d'environnement).

Démarrage rapide une fois `.env` configuré :

```bash
npm install
npm run dev
```
